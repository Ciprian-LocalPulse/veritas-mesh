{
  "name": "@veritas-mesh/dashboard",
  "version": "0.1.0-alpha",
  "private": true,
  "description": "Auditor & regulator interface for inspecting attestations and rule modules. No private data ever reaches this layer by protocol design.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^6.0.5",
    "typescript": "^5.5.4",
    "vite": "^8.2.0",
    "vitest": "^4.1.10",
    "@testing-library/react": "^16.0.0",
    "jsdom": "^24.1.1"
  }
}
