# `sdk/` — Per-Language SDKs

**Status: not started.** SDKs will be generated from [`proto/`](../proto/) once the schema is stabilized through the RFC process (see [`rfcs/README.md`](../rfcs/README.md)). Generating SDKs against the current draft schema would produce client code against a format that is expected to change.
