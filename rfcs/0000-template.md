# RFC NNNN: <Title>

- **Status:** Draft
- **Author(s):**
- **Discussion:** <link to the pull request once opened>

## Summary

One paragraph: what is being proposed, in plain language.

## Motivation

What problem does this solve? Why does the project need this change now, rather than later or never?

## Detailed Design

The actual technical content. Be as precise as the change warrants — a cryptographic-primitive RFC needs precision that a documentation-restructuring RFC does not. If this RFC changes a security-relevant property, explicitly state which property (referencing the relevant section of [`spec/THREAT_ANALYSIS.md`](../spec/THREAT_ANALYSIS.md) — e.g. §5 for a specific attack scenario, §6 for one of the three target properties — rather than assuming a fixed section number, since that document's structure may change) and how the change affects it.

## Drawbacks

Why might we *not* want to do this? An RFC with no drawbacks section, or one that only lists trivial drawbacks, has probably not been thought through adequately.

## Alternatives Considered

What else was considered, and why was it not chosen? Include the "do nothing" alternative explicitly.

## Impact on Existing Work

Does this require changes to already-published rule modules, the protocol spec, or any implemented code? Be specific about what breaks and what migration looks like.

## Open Questions

Anything this RFC does not resolve, to be tracked separately.
